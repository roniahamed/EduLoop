# version: '3.8'
services:
  web:
    build: .
    volumes:
      - .:/app
    
    ports:
      - "8011:8011"
    







# version: '3.8'

# services:
#   web:
#     build: .
#     command: gunicorn --config gunicorn.conf.py eduloop.wsgi:application
#     volumes:
#       - .:/app
#       - staticfiles:/app/staticfiles
#     ports:
#       - "8010:8010"
#     environment:
#       - DEBUG=False
#       - SECRET_KEY=${SECRET_KEY}
#       - DB_NAME=${DB_NAME}
#       - DB_USER=${DB_USER}
#       - DB_PASSWORD=${DB_PASSWORD}
#       - DB_HOST=db
#       - DB_PORT=5432
#     depends_on:
#       db:
#         condition: service_healthy
#     restart: unless-stopped
#     healthcheck:
#       test: ["CMD", "curl", "-f", "http://localhost:8010/"]
#       interval: 30s
#       timeout: 10s
#       retries: 3
#       start_period: 40s

#   db:
#     image: postgres:15
#     volumes:
#       - postgres_data:/var/lib/postgresql/data
#     environment:
#       - POSTGRES_DB=${DB_NAME}
#       - POSTGRES_USER=${DB_USER}
#       - POSTGRES_PASSWORD=${DB_PASSWORD}
#     ports:
#       - "5432:5432"
#     restart: unless-stopped
#     healthcheck:
#       test: ["CMD-SHELL", "pg_isready -U ${DB_USER} -d ${DB_NAME}"]
#       interval: 10s
#       timeout: 5s
#       retries: 5

# volumes:
#   postgres_data:
#   staticfiles:
